import React, { Component } from 'react';
import { connect } from 'react-redux';
import { toggleDetail } from '../redux/actions/actions';

class Whiskey extends Component {
  componentDidMount() {
    let {storage, }
  }
  render
}
